To make the logger show itself after it is hidden, make a form, with the caption set as 'Logger'

Make sure the caption is : Logger

Helpful, But Not Needed Things :

- Unload Form After 10s.
- Don't Show Form in taskbar (You'll see why if u play around with this)